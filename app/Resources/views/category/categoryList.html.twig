{% if categories %}
    <h3>Categories</h3>

    {% for category in categories %}
        <a class="btn btn-info buttons"  style="width: 100%" href="{{ path('category_show', { 'id' : category.id}) }}">{{ category.name }}</a>
        <br/>
    {% endfor %}
{% endif %}
